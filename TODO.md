# TODO (Fix Expo crash: RN host components)

## Step 1

- Convert `components/Dashboard.tsx` from DOM tags (`div`, `span`, `button`, etc.) to React Native primitives (`View`, `Text`, `Pressable`).

## Step 2

- Convert `components/MarketPlace.tsx` the same way (`h1/h2/p/div/span/button/input`).

## Step 3

- Convert `components/History.tsx` the same way.

## Step 4

- Convert `components/Profile.tsx` the same way.

## Step 5

- Convert `components/TopUpModal.tsx` the same way.

## Step 6

- Convert `components/VendorCard.tsx` the same way (includes `img`, `h3`, `p`, `span`, etc.).

## Step 7

- Convert `app/index.tsx` the same way (`div/nav/button`).

## Step 8

- Re-run `npx expo start` and verify the crash is gone.
## Step 7

- Convert `app/index.tsx` the same way (`div/nav/button`).

## Step 8

- Re-run `npx expo start` and verify the crash is gone.
